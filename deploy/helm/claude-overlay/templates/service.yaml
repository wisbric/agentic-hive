apiVersion: v1
kind: Service
metadata:
  name: {{ include "claude-overlay.fullname" . }}
  labels:
    {{- include "claude-overlay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "claude-overlay.selectorLabels" . | nindent 4 }}
